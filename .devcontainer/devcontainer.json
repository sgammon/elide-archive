{
    "build": {
        "dockerfile": "./Dockerfile",
        "context": ".."
    },
    "initializeCommand": "make build DEVCONTAINER=yes",
    "postAttachCommand": "gcloud auth login && make help"
}

